<section class="hero-section">
  <div class="hero-content">
    <h1>{{ section.settings.title }}</h1>
    <p>{{ section.settings.subtitle }}</p>
    {% if section.settings.button_text != blank %}
      <a href="{{ section.settings.button_link }}" class="hero-btn">
        {{ section.settings.button_text }}
      </a>
    {% endif %}
  </div>

  {% if section.settings.hero_image != blank %}
    <img 
      src="{{ section.settings.hero_image | image_url: width: 1500 }}" 
      alt="Hero Image" 
      class="hero-image"
      width="{{ section.settings.hero_image.width }}"
      height="{{ section.settings.hero_image.height }}"
    >
  {% endif %}
</section>

{% schema %}
{
  "name": "Hero Section",
  "settings": [
    { "type": "text", "id": "title", "label": "Title" },
    { "type": "text", "id": "subtitle", "label": "Subtitle" },
    { "type": "image_picker", "id": "hero_image", "label": "Hero Image" },
    { "type": "url", "id": "button_link", "label": "Button Link" },
    { "type": "text", "id": "button_text", "label": "Button Text" }
  ],
  "presets": [
    { "name": "Hero Section", "category": "Homepage" }
  ]
}
{% endschema %}

{% stylesheet %}
.hero-section {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 80px 20px;
  background-color: #f9f9f9;
}
.hero-section .hero-content h1 {
  font-size: 3rem;
  margin-bottom: 20px;
  font-weight: 700;
}
.hero-section .hero-content p {
  font-size: 1.2rem;
  margin-bottom: 30px;
  color: #555;
}
.hero-btn {
  background-color: #000;
  color: #fff;
  padding: 12px 30px;
  border-radius: 8px;
  text-decoration: none;
  font-weight: 600;
  transition: 0.3s ease;
}
.hero-btn:hover {
  background-color: #e60023;
}
.hero-image {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  opacity: 0.5;
  z-index: -1;
}
{% endstylesheet %}
{% comment %}
  This section displays a grid of collections with their images and descriptions.
{% endcomment %}

{% comment %}
  Usage:
  - Add this section to a page to showcase your collections in a grid layout.
  - Customize the grid item width and gap through the section settings.
{% endcomment %}